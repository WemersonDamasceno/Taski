import 'package:flutter/material.dart';

class AppConstants {
  static const databaseName = 'taski.db';

  static const openCreateTask = ValueKey('open_create_task');
  static const taskInput = ValueKey('task_input');
  static const noteInput = ValueKey('note_input');
  static const createTaskButton = ValueKey('create_task_button');
  static const refresh = ValueKey('refresh');
}
